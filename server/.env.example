# Copia estos valores como variables de entorno del servidor (no subas un .env real).
ADMIN_EMAIL=admin@tudominio.com
# Genera el hash con: node -e "require('bcrypt').hash('tu-clave-segura', 12).then(console.log)"
ADMIN_PASSWORD_HASH=$2b$12$REEMPLAZA_ESTE_HASH
JWT_SECRET=reemplaza-esto-por-un-secreto-largo-y-aleatorio
